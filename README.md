# VerySimpleHttpServer
A very simple http server implemented in pure C and based on Reactor model.

# get start
compile:
```
mkdir build
cd build
cmake ..
make
```

run:
```
cd ../release
sudo ./httpserver # or followed by a port num
```
